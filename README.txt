I added alt description to images for accessibility
I added semantic html elements like header, section, and footer
I fixed the SEO button on the header so it would drop the screen to matching box on the site like the other two buttons did
Changed the title from "website" to "Horiseon Digital Marketing"
